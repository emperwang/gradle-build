// 此时项目的总的一个配置
// 此配置是 项目的名称
rootProject.name = 'JavaGradle'

// 项目构建之前的钩子函数
gradle.beforeProject {
    println "setting.build: before project"
}

gradle.settingsEvaluated {
    println "setting.build : setting evaluated"
}

gradle.projectsLoaded {
    println "setting.build : project loaded"
}
